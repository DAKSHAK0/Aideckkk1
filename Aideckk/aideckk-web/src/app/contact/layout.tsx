import { type Metadata } from "next"

export const metadata: Metadata = {
    title: "Start a Project | AiDECKK",
    description: "Contact AiDECKK to discuss your custom web development project, e-commerce store, or premium landing page.",
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
    return children
}
