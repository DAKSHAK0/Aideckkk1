import { type Metadata } from "next"

export const metadata: Metadata = {
    title: "Services | AiDECKK Web Development",
    description: "Premium custom website development, e-commerce, and landing pages for ambitious brands.",
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
    return children
}
