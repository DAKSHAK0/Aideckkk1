import { type Metadata } from "next"

export const metadata: Metadata = {
    title: "Selected Work | AiDECKK Premium Web Studio",
    description: "A curated look at real websites we've built. High-performance custom web development for ambitious brands.",
}

export default function WorkLayout({ children }: { children: React.ReactNode }) {
    return children
}
